import setuptools

setuptools.setup(
    name="vou",
    version="0.0.1",
    packages=setuptools.find_packages(),
    description="Simulation of a virtual opioid user",
    python_requires=">=3.6",
    install_requires=["numpy"],
)
